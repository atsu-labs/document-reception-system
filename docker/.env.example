# Environment variables for Docker development

# JWT secret for backend
JWT_SECRET=your-secret-key-change-in-production

# Database path (for SQLite in Docker)
DATABASE_PATH=/app/backend/data/local.db
