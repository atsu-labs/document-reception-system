{
  "name": "document-reception-system",
  "version": "1.0.0",
  "private": true,
  "description": "Document Reception Management System",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.61.0"
  }
}
